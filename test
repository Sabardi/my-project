<template>
    <section class="bg-white">
        <!-- <img :src="product.image" alt="" class="mx-auto"> -->
        <!-- footer -->
        <section class="p-6 text-center uppercase">
            <h1 class="text-xl font-bold">Nama prduk</h1>
            <!-- <p>{{ product.description }}</p>
            <p>{{ product.price }}</p>
            <p>{{ product.description }}</p> -->
        </section>
    </section>
</template>


<script setup>
import { onMounted } from 'vue';
import { useRoute, useRouter } from 'vue-router';


// reff
const ref = ref([])

// store

// initial
const route = useRoute()


onMounted(async () => {
    const response = await fetch(`https://fakestoreapi.com/products/id dari parameter nya}`);
    console.log('data',response);
    // const data = await response.json();
    // products.value = data;
});
</script>